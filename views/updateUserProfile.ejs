<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Create Task</title>
		<link rel="stylesheet" href="./stylesheets/post.css" />
	</head>
	<body>
		<div id="app">
			<h1 id="app-heading">Create a Task</h1>
			<div id="app-content">
				<div class="container">
					<form id="task-form" method="put" action="/updateUserProfile">
						<div class="form-group">
							<% if(typeof info !== 'undefined') { %>
							<p
								style="
									font-size: 18px;
									text-align: center;
									color: rgb(236, 187, 114);
								"
							>
								<%= info %>
							</p>

							<% } %>
							<label for="task-title">userId:</label>
							<input type="text" id="task-title" name="id" required />
						</div>
						<div class="form-group">
							<label for="task-title">fName:</label>
							<input type="text" id="task-title" name="fName" required />
						</div>
						<div class="form-group">
							<label for="task-description">lname:</label>
							<input type="text" id="task-title" name="lName" required />
						</div>
						<div class="form-group">
							<label for="task-date">email</label>
							<input type="email" id="task-date" name="email" required />
						</div>
						<div class="form-group">
							<label for="task-title">password:</label>
							<input type="password" id="task-title" name="password" required />
						</div>
						<% if (typeof errors !== 'undefined') { %>
						<div class="error-message">
							<p style="color: rgb(135, 213, 239); font-size: 18px">
								<%= errors %>
							</p>
						</div>
						<% } %>
						<button type="submit" class="submit-button">Submit Task</button>
					</form>
					<div id="confirmation-message" class="hidden">
						<span class="checkmark">&#10003;</span>
						<p style="color: #333">Your post has been submitted!</p>
					</div>
				</div>
			</div>
		</div>
		<script>
			let isFormDirty = false
			function markFormAsDirty() {
				isFormDirty = true
			}
			function markFormAsClean() {
				isFormDirty = false
			}

			window.addEventListener('beforeunload', function (e) {
				if (isFormDirty) {
					e.preventDefault()
					e.returnValue = ''
				}
			})
		</script>
	</body>
</html>
fName, lName, email, password
